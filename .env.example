# Supabase project (Project Settings -> API)
# Only public values belong here: the URL and the ANON key.
# Never put the service_role key in a Vite app — it gets shipped to the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=